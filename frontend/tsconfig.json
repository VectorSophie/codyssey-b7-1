{
    // 브라우저에서 실행할 React 코드의 TypeScript 설정을 연결한다.
    "files": [],
    // 앱 코드와 Vite 설정을 각각 독립적으로 검사한다.
    "references": [
        // React 앱 코드 설정을 사용한다.
        { "path": "./tsconfig.app.json" },
        // Vite 설정 코드의 Node 환경 설정을 사용한다.
        { "path": "./tsconfig.node.json" }
    ]
}
